# Shared analysis stack (Task I + Task II plotting/fits)
tqdm>=4.66
pandas>=2.0
matplotlib>=3.7
numpy>=1.24,<2
# scipy 1.11+ needs Python 3.9+; p4lang/p4c image ships Python 3.8 (use 1.10.x there).
scipy>=1.10
statsmodels>=0.14

# Task II control plane (capture, P4Runtime)
scapy
p4runtime
grpcio>=1.50
